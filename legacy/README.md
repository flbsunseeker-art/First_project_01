# Legacy Prototype

This folder contains the old Streamlit prototype, root-level compatibility wrappers,
and historical migration helpers.

The active StockPilot application is now:

- Backend: `apps/api`
- Frontend: `apps/web`
- Startup: `start.command`

These files are kept temporarily so they can be moved out of the project later.
